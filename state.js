function showDetails(imageElement) {
    var details = imageElement.nextElementSibling;
    details.style.display = 'block';
  }
  
  function hideDetails(imageElement) {
    var details = imageElement.nextElementSibling;
    details.style.display = 'none';
  }